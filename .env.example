# Optional environment variables for SynAI

# Application title (optional - defaults to "SynAI")
VITE_APP_TITLE=SynAI

# Default AI provider (optional - defaults to "openai")
# Options: openai, anthropic, perplexity
VITE_DEFAULT_PROVIDER=openai

# Enable debug logging (optional - defaults to false in production)
VITE_ENABLE_DEBUG=false

# Development server port (optional - defaults to 5173)
PORT=5173